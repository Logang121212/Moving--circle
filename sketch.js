//function is a box of code that does things that are connected
function setup() {
  createCanvas(600, 600)
  
}

function draw() {
   noStroke();
  fill("yellow");
  background(225, 20, 90);
  circle(mouseX, mouseY, 20);
}
